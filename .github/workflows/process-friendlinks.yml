name: 处理友链申请

on:
  issues:
    types: [opened, edited]
  schedule:
    - cron: '0 16 * * *'  # 北京时间每天 0 点（UTC+8）
  workflow_dispatch:

permissions:
  contents: write
  issues: write

jobs:
  process-friendlinks:
    runs-on: ubuntu-latest
    steps:
      - name: 检出主分支
        uses: actions/checkout@v4
        with:
          path: main
        
      - name: 检出 output 分支
        uses: actions/checkout@v4
        with:
          ref: output
          path: output
          
      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      - name: 安装依赖
        run: |
          pip install feedparser requests beautifulsoup4 python-dateutil PyYAML
          
      - name: 处理友链
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          EVENT_NAME: ${{ github.event_name }}
          MAIN_PATH: main
          OUTPUT_PATH: output
        run: python main/scripts/process_friendlinks.py
        
      - name: 提交更改到 output 分支
        run: |
          cd output
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add v2/data.json
          git diff --quiet && git diff --staged --quiet || git commit -m "🤖 自动更新友链数据 $(date +'%Y-%m-%d %H:%M:%S')"
          git push